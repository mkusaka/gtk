# TODO

## Next

- Add release packaging beyond local `npm link`.
- Add task list create/update/delete commands if the product needs full list lifecycle management.
- Decide whether `auth credentials use <name>` is worth adding for multi-client switching.
- Add integration tests around mocked OAuth refresh and Tasks API failure responses.
- Decide whether to support manual auth-code paste for headless environments.

## Later

- Evaluate multi-account UX beyond `--account <email>`.
- Evaluate publish strategy for npm or Homebrew.
- Add clearer machine-readable error codes if other tools will wrap `gtk`.
